# Import needed libraries here

interval = 60 # Interval to poll at in seconds

# Define the "get" function to be run
def get():
    # Get whatever value it is

    return {'name': 'result', 'value': 'Done!'} # MUST return a value (And in dictionary format with the keys of "name" and "value")